{
  "mcpServers": {
    "privacyguard": {
      "command": "./target/release/privacyguard-mcp",
      "args": ["--config", "config.toml"],
      "env": {
        "RUST_LOG": "info"
      }
    }
  }
}
